#!/bin/sh

# Release runtime configuration.
# See https://hexdocs.pm/mix/Mix.Tasks.Release.html#module-vm-args-and-env-sh-eex

# Sets the release to work across nodes.
# RELEASE_DISTRIBUTION must be "sname" (short), "name" (long), or "none".
export RELEASE_DISTRIBUTION="none"

# Set the Erlang VM memory limits.
# Fly.io shared-cpu-1x with 512MB RAM — keep some headroom for OS.
export ERL_FLAGS="+JPperf true"
