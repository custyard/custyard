# Local container testing / deployment reference
# Usage: docker compose up --build
services:
  custyard:
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "4000:4000"
    volumes:
      - custyard-data:/data
    environment:
      PHX_HOST: localhost
      PORT: "4000"
      DATABASE_PATH: /data/custyard.db
      # SECRET_KEY_BASE: "generate-with-mix-phx.gen.secret"
      # OPERATOR_PASSWORD: "set-a-stable-password-here"
      # MAIL_ADAPTER: smtp
      # LMTP_ENABLED: "false"
      # IMAP_ENABLED: "false"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

volumes:
  custyard-data:
