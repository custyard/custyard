# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# Inside the script, you can read and write to any of your
# repositories directly:
#
#     Custyard.Repo.insert!(%Custyard.SomeSchema{})
#
# We recommend using the bang functions (`insert!`, `update!`
# and so on) as they will fail if something goes wrong.

# NOTE: In dev, the application auto-creates an operator account
# (admin@custyard.local) on each boot - see Custyard.Application.
# This seeds file currently contains no additional seed data.
#
# Add organization, contact, or conversation seed data here if
# needed for development/testing scenarios.
